# Railway Configuration for RepoIQ Monorepo
# NOTE: This is a monorepo with Backend and Frontend as separate services.
# You MUST set the Root Directory in Railway service settings:
# - Backend service: Root Directory = "Backend"
# - Frontend service: Root Directory = "Frontend"
# 
# This root-level file is informational only.
# Railway will use Backend/railway.toml and Frontend/nixpacks.toml when Root Directory is set correctly.
